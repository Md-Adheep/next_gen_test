version: "3"

services:

  app:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - mysql_db

  mysql_db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: nextgen_db
      MYSQL_USER: appuser
      MYSQL_PASSWORD: Str0ngP@ssw0rd!2026
